#include <jasmine/jasmine.h>
#include <image/RGBAImage.h>

void main() {
    Describe("RGBAImage<byte>", func() {
        Expect(true).ToBeTruthy();
        It("should be able to read data from bmp file", func() {
            RGBAImage<byte> image("TestResources/Lena.bmp");
            Expect(image.Height()).ToBe(512);
            Expect(image.Width()).ToBe(512);
        });
		Describe("when read lena.bmp finished",func(){
			  RGBAImage<byte> image("TestResources/Lena.bmp");
				It("should be able to get pixel",func(){
		});
		});
    });
}